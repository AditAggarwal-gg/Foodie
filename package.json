{
  "name": "foodie-voice-order-server",
  "version": "1.0.0",
  "type": "module",
  "description": "Backend for the voice-ordering demo — serves the frontend and proxies voice parsing to a free LLM (local Ollama or hosted Groq).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.1"
  }
}
